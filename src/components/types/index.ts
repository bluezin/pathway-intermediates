export interface Specie {
  link: string;
  id: string;
  description: string;
  imgForTitle: string;
}

export interface CategoryData {
  link: string;
  id: string;
  description: string;
  imgForTitle: string;
}
